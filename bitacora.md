# BitÃ¡cora de Desarrollo: Sistema de Alertas de Apuestas Multideporte

## Objetivo del Proyecto
Crear un sistema automatizado multideporte (FÃºtbol + MLB BÃ©isbol) que envÃ­e alertas basadas en eventos en vivo (tarjetas rojas, empates al medio tiempo de favoritos, ventajas de underdogs, cÃ³rneres, partidos calientes y entradas clave de bÃ©isbol) con seguimiento automatizado GREEN/RED post-partido.

## Estado Actual
- VersiÃ³n 2.0.0: Clon funcional idÃ©ntico de la versiÃ³n 1.9.0 (persistencia de seguimiento de jugadas y auto-suscripciÃ³n de usuario principal al arrancar). Se descartan los agregados de las versiones v1.10.x y v1.11.x.
- Sistema de 7 reglas de fÃºtbol completado y verificado. Servidor listo con redeploy automÃ¡tico a Railway.

## Decisiones Tomadas
- **Fuentes de datos:** 
  - FÃºtbol: `v3.football.api-sports.io`
  - BÃ©isbol (MLB): `v1.baseball.api-sports.io` (misma API Key).
- **Plataforma:** Bot de Telegram en Node.js.
- **Reglas FÃºtbol (1-7):** Tarjeta Roja, Favorito Sufre HT, Sorpresa Tempranera, Asedio Late Goal, HT Comeback (Top), Late Corners (Top), Partido Caliente (Top).
- **Reglas BÃ©isbol MLB (1-3):** 
  - Regla MLB 1: Favorito en Apuros al Medio Juego (Inning 3-5).
  - Regla MLB 2: Cierre Apretado / Tension (Inning 7-9).
  - Regla MLB 3: FestÃ­n de Carreras (Early Over Inning 1-3).
- **Seguimiento Post-Partido:** El bot rastrea cada alerta emitida de fÃºtbol y bÃ©isbol y envÃ­a automÃ¡ticamente un mensaje de veredicto GREEN ðŸŸ© / RED ðŸŸ¥ al silbatazo final o cierre del inning 9.

## Historial de Cambios
- **[2026-07-21]**: CreaciÃ³n de la bitÃ¡cora y anÃ¡lisis inicial de requerimientos.
- **[2026-07-21]**: ImplementaciÃ³n del cÃ³digo base en Node.js (`apiClient.js`, `rulesEngine.js`, `index.js`). Pruebas de simulaciÃ³n exitosas.
- **[2026-07-21]**: CreaciÃ³n de `config.js` para filtrado por ligas principales. ImplementaciÃ³n de Reglas 5, 6 y 7, junto con `evaluateAlertResults` (GREEN/RED).
- **[2026-07-22]**: CreaciÃ³n de los mÃ³dulos de MLB BÃ©isbol (`baseballApiClient.js`, `baseballRulesEngine.js`, `testBaseball.js`). IntegraciÃ³n multideporte en `index.js` y desplegado en Railway.
- **[2026-07-22]**: ResoluciÃ³n de Error HTTP 429 (Rate Limit de API-Sports). Se implementÃ³ un sistema de cachÃ© negativo (`NO_ODDS`), filtrado de partidos en vivo para evitar consultas redundantes de partidos finalizados y cooldown automÃ¡tico de 60 segundos ante status 429 en `apiClient.js` y `baseballApiClient.js`. ReducciÃ³n masiva de peticiones API de ~50+/min a ~2-4/min. Pruebas de integraciÃ³n exitosas.
- **[2026-07-22]**: IntegraciÃ³n de Recomendaciones de Apuestas y Cuota Objetivo `@1.60 o mÃ¡s`. Se actualizaron los motores de reglas (`rulesEngine.js` y `baseballRulesEngine.js`) para incluir la recomendaciÃ³n directa del mercado a operar (ej: Doble Chance, Over 0.5 2HT, CÃ³rneres Totales, HÃ¡ndicap) junto con la cuota objetivo sugerida `@1.60 o mÃ¡s`. Pruebas en `test.js` y `testBaseball.js` verificadas.
- **[2026-07-22]**: RestricciÃ³n de Horario de Monitoreo (7 AM - 9 PM Hora Centro MÃ©xico). Se configurÃ³ `America/Mexico_City` y `isWithinActiveHours()` en `config.js` e `index.js` para ejecutar el polling por cron Ãºnicamente de 07:00 a 21:00 hrs CST/CDT, previniendo el consumo innecesario de peticiones API durante la noche/madrugada.
- **[2026-07-22]**: Despliegue Exitoso a ProducciÃ³n (`main`). Se enviaron todos los cambios y mejoras (soluciÃ³n Rate Limit 429, cuotas objetivo @1.60 y restricciÃ³n de horario 7 AM - 9 PM CDMX) al repositorio GitHub, activando el despliegue automÃ¡tico en Railway.
- **[2026-07-22]**: InvestigaciÃ³n sobre Alertas de MLB. Se detectÃ³ que la API de bÃ©isbol (`v1.baseball.api-sports.io`) con el plan gratuito ("Free Plan") bloquea el acceso a la temporada actual devolviendo el error: `Free plans do not have access to this season, try from 2022 to 2024.` Adicionalmente, se identificÃ³ que el horario restrictivo de monitoreo (7 AM - 9 PM CDMX) corta el seguimiento de partidos nocturnos de la MLB que finalizan mÃ¡s tarde.
- **[2026-07-23]**: IntegraciÃ³n de Inteligencia Artificial (Gemini) para anÃ¡lisis y recomendaciones dinÃ¡micas. Se creÃ³ el mÃ³dulo `aiService.js` para consumir la API de REST de Gemini con Axios de forma nativa. Se implementÃ³ un sistema de rotaciÃ³n de mÃºltiples claves y fallback de modelo (`gemini-flash-latest` -> `gemini-2.5-flash`). Se diseÃ±aron prompts irreverentes e informales en espaÃ±ol para fÃºtbol y MLB. Para resolver el truncado por consumo de tokens del razonamiento interno (Thinking), se elevÃ³ el lÃ­mite a `maxOutputTokens: 4000` y el timeout a 20 segundos. Se integrÃ³ la llamada en `index.js` para fÃºtbol y bÃ©isbol reemplazando dinÃ¡micamente las recomendaciones fijas con fallbacks tolerantes a errores.
- **[2026-07-23]**: Enriquecimiento y formateo de estadÃ­sticas y eventos para la IA. Se implementÃ³ un sistema de consulta dinÃ¡mica on-demand para fÃºtbol en `index.js` que descarga la informaciÃ³n detallada de estadÃ­sticas y eventos al activarse cualquier alerta, evitando consumos innecesarios en el polling pasivo. Se crearon funciones de formateo para fÃºtbol (`formatFootballStats` y `formatFootballEvents`) y para bÃ©isbol (`formatBaseballStats`) que traducen las estructuras JSON de API-Sports en resÃºmenes textuales de alta legibilidad y bajo consumo de tokens. Se ajustaron los prompts de Gemini en `aiService.js` para obligar al modelo a usar y referenciar estas mÃ©tricas reales (posesiÃ³n, tiros, cÃ³rneres, hits, errores, desglose por entradas) en sus anÃ¡lisis jocosos e irreverentes. Pruebas integradas en `testAi.js` completadas exitosamente.
- **[2026-07-24]**: AnÃ¡lisis HistÃ³rico de 5 Partidos e Inteligencia Artificial en Tono Formal. Se implementÃ³ la funciÃ³n `getTeamLastMatches` en `apiClient.js` para consultar los Ãºltimos 5 partidos jugados de cualquier equipo en fÃºtbol. Se integrÃ³ esta lÃ³gica en `index.js` para consultar dinÃ¡micamente dichos partidos (con delays entre peticiones para control de tasa) al saltar una alerta, adjuntÃ¡ndolos a `matchData` para alimentar el prompt. Se agregaron y actualizaron los prompts en `aiService.js` para cambiar el estilo de anÃ¡lisis a un formato formal, objetivo y tÃ©cnico (para fÃºtbol y bÃ©isbol MLB), eliminando el tono jocoso e irreverente original. Se actualizÃ³ el archivo de pruebas `testAi.js` agregando los mock de partidos histÃ³ricos de fÃºtbol, validando exitosamente el comportamiento formal y robustecido en `testAi_output.txt`. **Adicionalmente, se corrigiÃ³ un sesgo de recomendaciÃ³n por defecto en `index.js` mediante la extracciÃ³n de `cleanRuleDetails`, eliminando las recomendaciones estÃ¡ticas de las alertas de fÃºtbol y bÃ©isbol para garantizar que la IA realice una evaluaciÃ³n tÃ¡ctica e independiente basada exclusivamente en datos estadÃ­sticos e histÃ³ricos reales.**
- **[2026-07-24]**: OptimizaciÃ³n de Consumo y Throttling de API-Sports (OpciÃ³n B). Se resolviÃ³ el sobreconsumo que agotÃ³ las 7,500 peticiones del plan PRO. Se optimizÃ³ `needsEvents` en `rulesEngine.js` para que solo determine consultar la API si el partido cumple las pre-condiciones del marcador (empate o ventaja del underdog para Tarjeta Roja). TambiÃ©n se implementÃ³ un sistema de Throttling con cooldown de 4 minutos y almacenamiento en cachÃ© en memoria (`eventsCache` y `statsCache`) en `index.js`, garantizando la reducciÃ³n del consumo y la reutilizaciÃ³n segura de datos recientes en lugar de enviar arreglos vacÃ­os. Se integrÃ³ un mecanismo automÃ¡tico de limpieza de cachÃ©s al finalizar partidos para evitar fugas de memoria.
- **[2026-07-25]**: IntegraciÃ³n del Sistema de Parlays (Pre-partido y En Vivo). Se modificaron los prompts en `aiService.js` para exigir la respuesta con un nivel de confianza estimado (`ðŸ”¥ Confianza Estimada: [Porcentaje]%`). Se implementÃ³ `generateDailyParlay` para armar combinadas de alta certeza utilizando datos de momios e historial reciente de equipos seleccionados. En `index.js`, se programaron tres tareas cron (8:30 AM, 9:30 AM y 10:30 AM) para publicar el Parlay del DÃ­a con partidos del dÃ­a no iniciados de ligas importantes. Asimismo, se implementÃ³ un sistema de cola en memoria (`liveAlertsQueue` con expiraciÃ³n de 40 minutos) que detecta y combina automÃ¡ticamente 2 o mÃ¡s alertas en vivo de alta confianza (>= 85%) en un Parlay en Vivo dinÃ¡mico de Telegram. Se actualizaron las pruebas en `testAi.js` y se verificÃ³ el correcto funcionamiento. **Adicionalmente, se actualizÃ³ la versiÃ³n del bot a v1.5.0 en package.json y se integrÃ³ de forma dinÃ¡mica en el comando /start de Telegram en index.js para facilitar la verificaciÃ³n visual de la versiÃ³n instalada. Finalmente, se solucionÃ³ un error lÃ³gico crÃ­tico de la IA (alucinaciÃ³n de momios en vivo) mediante la integraciÃ³n de una regla estricta de realismo en los prompts de aiService.js, evitando la recomendaciÃ³n de mercados casi resueltos con cuotas altas (ej: Doble Oportunidad a favor del equipo que ya va ganando) y forzando a Gemini a proponer mercados lÃ³gicos de reacciÃ³n (1X/X2 del que va perdiendo, ML, prÃ³ximo gol, Over de goles).**
- **[2026-07-25]**: CorrecciÃ³n del Sistema de Veredictos GREEN/RED con IA. Se solucionÃ³ el fallo donde las alertas con predicciones personalizadas de IA se calificaban errÃ³neamente usando la validaciÃ³n estÃ¡tica original. Se implementÃ³ la funciÃ³n `evaluatePredictionOutcome` en `aiService.js` para consultar a Gemini y evaluar de forma dinÃ¡mica y contextual la recomendaciÃ³n real enviada al canal basada en el marcador final, estadÃ­sticas y eventos. En `index.js`, se modificÃ³ el guardado en cachÃ© de alertas para registrar la propiedad `aiRecommendation` y se hizo asÃ­ncrona la resoluciÃ³n de veredictos post-partido (`evaluateAlertResults` y `evaluateBaseballAlertResults`). Se incluyÃ³ un fallback automÃ¡tico y transparente hacia el motor estÃ¡tico cuando no hay recomendaciÃ³n de IA o falla la API. Se actualizaron las pruebas en `test.js`, `testBaseball.js` y se validÃ³ en `testAiEvaluation.js` tanto para veredictos RED como GREEN.
- **[2026-07-26]**: AnÃ¡lisis de rendimiento de estrategias basado en el archivo de mensajes de Telegram (`messages.html`). Se implementÃ³ un script de parseo secuencial (`analyze.js`) que extrajo 182 veredictos histÃ³ricos totales (con un 79.12% de aciertos global) y 79 veredictos del dÃ­a anterior, 25 de julio de 2026 (con un 82.28% de aciertos). Se creÃ³ un reporte interactivo premium en HTML (`reporte_rendimiento.html`) con un diseÃ±o moderno responsivo en modo oscuro que presenta grÃ¡ficos y desgloses de rendimiento por estrategia, clasificando cuÃ¡les funcionan con alto acierto (como "Tarjeta Roja EstratÃ©gica" y "Partido Caliente / Tarjetas") y cuÃ¡les requieren revisiÃ³n crÃ­tica (como "HT Comeback Favorito").
- **[2026-07-26]**: IntegraciÃ³n de Descarte Inteligente de Apuestas por IA. Se actualizaron los prompts en [aiService.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/aiService.js) para instruir a la IA a que recomiende omitir apuestas de alto riesgo con la frase "Evitar apuesta / No recomendada". En [rulesEngine.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/rulesEngine.js) and [baseballRulesEngine.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/baseballRulesEngine.js), se modificÃ³ la resoluciÃ³n de veredictos post-partido para interceptar este descarte de la IA, emitiendo un reporte neutral `âšª APUESTA EVITADA` en Telegram con una comparativa sobre si la sugerencia evitÃ³ un fallo (RED) o priorizÃ³ la precauciÃ³n sobre un acierto (GREEN), y marcÃ¡ndolo en metadatos como `isOmitted: true` para fines de mediciÃ³n de rendimiento real. Se validaron los cambios exitosamente en [test_omission.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/test_omission.js).
- **[2026-07-26]**: ImplementaciÃ³n del **Sistema SafeOdds** de retenciÃ³n y monitoreo de alertas en tiempo real. Se integrÃ³ una cola en memoria `pendingAlertsQueue` y soporte para consultar el endpoint `/odds/live` de API-Football cada minuto. Se implementÃ³ la lÃ³gica de mapeo inteligente de recomendaciones a cuotas activas (`getLiveOddForRecommendation`) y un estimador matemÃ¡tico de cuotas en vivo como fallback secundario por tiempo de espera (`waitMinutes`) ante la ausencia de datos en vivo de la API. Se programÃ³ la validaciÃ³n minuto a minuto en `processPendingAlerts()`, de modo que si hay cambio de marcador antes de alcanzar la cuota objetivo se cancela la alerta automÃ¡ticamente, y de lo contrario se envÃ­a al canal de Telegram al alcanzar el momio mÃ­nimo sugerido (ej. @1.60+). Se creÃ³ y validÃ³ el script de simulaciÃ³n `testSafeOdds.js` aprobando los escenarios de disparo por cuota, disparo por tiempo estimado y cancelaciÃ³n por gol.
- **[2026-07-26]**: RediseÃ±o estÃ©tico y de formato premium para las notificaciones de Telegram (fÃºtbol y bÃ©isbol MLB). Se simplificaron los encabezados de los partidos compactando el tiempo, marcador e inning en una sola lÃ­nea horizontal. Se envolvieron los datos clave del evento con separadores de lÃ­nea gruesa (`â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�â”�`). Se implementÃ³ un reformateador de la predicciÃ³n de IA en `index.js` que desglosa la respuesta de Gemini en una estructura elegante con iconos especÃ­ficos y negritas uniformes. Se robustecieron las expresiones regulares de extracciÃ³n en `extractConfidence` y `handleLiveParlayQueue` para soportar formatos ricos con asteriscos y negritas de Markdown. Se validÃ³ visualmente el resultado con el script de pruebas `testRender.js`.
- **[2026-07-26]**: DesactivaciÃ³n y remociÃ³n del monitoreo de bÃ©isbol (MLB). Se comentaron las importaciones de bÃ©isbol en `index.js`, se borraron las variables de cachÃ© asociadas y se eliminaron por completo las funciones de anÃ¡lisis `checkBaseballMatches` y `checkFinishedBaseballMatches`. TambiÃ©n se removieron sus ejecuciones en el programador cron minuto a minuto y en la inicializaciÃ³n de arranque, orientando el bot actual exclusivamente al anÃ¡lisis de fÃºtbol.
- **[2026-07-26]**: CorrecciÃ³n en el mapeo de cuotas en vivo de fÃºtbol en `getLiveOddForRecommendation()`. Se introdujo la discriminaciÃ³n estricta de palabras clave para que el bot ignore bÃºsquedas de cuotas si la recomendaciÃ³n es de Tarjetas o CÃ³rneres (retornando `null` de forma segura para usar el estimador de tiempo/fallback en su lugar). Para recomendaciones de Goles, se implementÃ³ la extracciÃ³n automÃ¡tica del nÃºmero de lÃ­nea de goles y su comparaciÃ³n exacta con la propiedad `handicap` en los valores de la API, evitando que el bot lea cuotas de lÃ­neas de goles muy avanzadas de forma errÃ³nea.
- **[2026-07-26]**: Ajuste en el SafeOdds System. Se fijÃ³ la cuota objetivo (`targetOdd`) en `1.60` de forma constante para todas las alertas. Esto permite que el bot envÃ­e la alerta al canal de Telegram inmediatamente cuando el momio en vivo alcance o supere `@1.60`, en lugar de esperar a que la cuota suba hasta el momio sugerido de la IA (ej. @1.95), previniendo que un gol temprano antes de alcanzar la cuota alta cancele la alerta.
- **[2026-07-26]**: CorrecciÃ³n CrÃ­tica en la EvaluaciÃ³n de Veredictos (Rate Limit Exhaustion). Se resolviÃ³ el fallo de "veredicto no enviado a partidos terminados". Se descubriÃ³ que partidos cancelados, suspendidos o aplazados (`CANC`, `PST`, `ABD`) se quedaban estancados en memoria sin ser eliminados, causando que el bot consultara repetitivamente la API cada minuto y agotara su cuota de peticiones. Se implementÃ³ una eliminaciÃ³n dinÃ¡mica de partidos con estatus problemÃ¡ticos. Adicionalmente, se protegiÃ³ la evaluaciÃ³n iterativa (`checkFinishedMatches`) con bloques `try...catch` para evitar que una excepciÃ³n por falta de momios (TypeError) en una alerta (como *Sorpresa tempranera*) abortara el proceso de evaluaciÃ³n de todos los demÃ¡s partidos. Finalmente, se excluyÃ³ de evaluaciÃ³n toda alerta que el sistema SafeOdds nunca alcanzÃ³ a enviar al canal de Telegram.
- **[2026-07-27]**: OptimizaciÃ³n CrÃ­tica de Latencia y Velocidad en EnvÃ­o de Alertas. Se solucionÃ³ el retraso percibido en el envÃ­o de alertas de fÃºtbol. En `index.js`, se paralelizÃ³ la obtenciÃ³n asÃ­ncrona de estadÃ­sticas, eventos y partidos recientes usando `Promise.all` para evitar el bloqueo secuencial. AdemÃ¡s, se reescribiÃ³ la lÃ³gica de integraciÃ³n de *SafeOdds*: si el mercado recomendado no es monitorizable (como tarjetas o cÃ³rneres) o si el partido no tiene cobertura de cuotas en vivo en la API, la alerta se envÃ­a de inmediato, en lugar de encolarla artificialmente. Para alertas que sÃ­ se encolan esperando alcanzar una cuota de `@1.60`, se fijÃ³ un lÃ­mite mÃ¡ximo de retenciÃ³n de 8 minutos para el estimador de tiempo. Finalmente, se eliminÃ³ el `cron` de 1 minuto para el monitoreo de partidos (`checkMatches`) y se reemplazÃ³ por un bucle asÃ­ncrono dinÃ¡mico protegido que sondea los partidos cada **30 segundos** en horario activo. Se incrementÃ³ la versiÃ³n a `1.5.6`.
- **[2026-07-28]**: AnÃ¡lisis retrospectivo de alertas y veredictos en `messages.html` (desde el 26 de julio a las 5:00 PM). Se desarrollÃ³ un script en Node.js (`scratch/analyze_messages.js`) que extrajo 38 alertas de reglas y 32 veredictos, logrando cruzar con Ã©xito 31 de ellas. Se ejecutÃ³ una simulaciÃ³n financiera partiendo de un capital inicial de $5,000 y apostando $100 a cada recomendaciÃ³n (excluyendo alertas no recomendadas por la IA), finalizando con un balance de $4,900. Se construyÃ³ una interfaz interactiva premium `reporte_rendimiento.html` con diseÃ±o oscuro y acordeones interactivos CSS para desplegar los partidos al hacer clic en cada regla.
- **[2026-07-28]**: AmpliaciÃ³n del anÃ¡lisis a la totalidad del historial de mensajerÃ­a en `messages.html`. Se actualizÃ³ el script `analyze_messages.js` para suprimir las restricciones de fecha y procesar el 100% de los datos (273 alertas totales y 234 veredictos). Se reestructurÃ³ el script para automatizar la generaciÃ³n y regeneraciÃ³n dinÃ¡mica del reporte HTML `reporte_rendimiento.html`, inyectando programÃ¡ticamente todas las estadÃ­sticas reales, acordiones de reglas con sus correspondientes tablas de partidos (220 cruces exitosos) y la simulaciÃ³n del balance de transacciones. La simulaciÃ³n global arrojÃ³ un balance final de $11,206.00 partiendo de $5,000 iniciales, con una efectividad global del 75.12% en las recomendaciones recomendadas.
- **[2026-07-29]**: IntegraciÃ³n del Sistema de Control Financiero Diario y Veredictos Web AutÃ³nomos (VersiÃ³n 1.6.0). Se creÃ³ el mÃ³dulo `financialTracker.js` y el archivo `financial_tracker.json` para llevar un balance persistente de las jugadas del bot, iniciando con un capital base of $5,000 MXN y apuestas de $250 MXN (5%) por recomendaciÃ³n (excluyendo descartes "Evitar apuesta"). Se interceptaron los 4 puntos de envÃ­o de alertas en `index.js` y la evaluaciÃ³n de veredictos post-partido en `checkFinishedMatches` para registrar y actualizar jugadas en tiempo real. En `aiService.js`, se implementÃ³ el mÃ©todo `resolveVerdictViaWeb` que utiliza Gemini 2.5 Flash con Google Search Grounding (`google_search`) para resolver veredictos de partidos pasados a travÃ©s de bÃºsquedas en la web cuando falten datos en la API de fÃºtbol. Por Ãºltimo, se programÃ³ un cron a las 7:35 AM (Hora Centro MÃ©xico) en `index.js` para depurar jugadas pendientes (API + Web) y enviar el reporte financiero diario de efectividad y balance de ayer y del acumulado semanal desde el domingo.
- **[2026-07-30]**: IntegraciÃ³n del Contexto de CompeticiÃ³n y Marcador Global en la IA (VersiÃ³n 1.7.0). Se modificÃ³ `index.js` para extraer y enviar las propiedades `leagueName` y `leagueRound` dentro de `matchData` al solicitar predicciones a la IA. Se actualizaron los prompts en `aiService.js` para fÃºtbol y bÃ©isbol, instruyendo a Gemini a clasificar explÃ­citamente el partido en tres escenarios tÃ¡cticos: eliminatoria de ida y vuelta (calculando el marcador global lÃ³gicamente a partir del partido previo del historial recente), localÃ­a neutral (ignorando la ventaja tÃ­pica de local), y torneos o ligas normales (evaluando la localÃ­a estÃ¡ndar y la urgencia de sumar los 3 puntos). Por Ãºltimo, se modificaron los mocks de prueba en `testAi.js` y se aÃ±adiÃ³ un nuevo caso de prueba para una eliminatoria de vuelta de la UEFA Champions League, validando con Ã©xito (en `testAi_output.txt`) que la IA deduce el marcador global de 3-2 en contra del Real Madrid y emite la recomendaciÃ³n de buscar el gol de manera tÃ¡ctica, mientras que en partidos normales de liga evalÃºa la urgencia estÃ¡ndar sin alucinar marcadores acumulados.
- **[2026-07-30]**: Re-despliegue correctivo y actualizaciÃ³n a v1.7.1 por error detectado en el despliegue automÃ¡tico de Railway, forzando un nuevo build a travÃ©s de GitHub tras corroborar que las pruebas locales de sintaxis y respuesta de IA pasan satisfactoriamente.
- **[2026-08-01]**: ActualizaciÃ³n de credenciales de Gemini en el archivo `.env`. Se reemplazaron las claves API de la cuenta con facturaciÃ³n por las nuevas claves gratuitas proporcionadas por el usuario para migrar al plan sin costos (Free Tier).
- **[2026-08-01]**: IntegraciÃ³n de DeepSeek para anÃ¡lisis predictivo dual en vivo. Se configurÃ³ la variable de entorno `DEEPSEEK_API_KEY` en el archivo `.env` y se crearon las funciones `callDeepSeekWithRotation` y `generatePredictionDeepSeek` en `aiService.js` con soporte para modelos `deepseek-v4-flash` and `deepseek-v4-pro`. Se adaptÃ³ `index.js` para realizar consultas paralelas protegidas a ambos proveedores de IA y dar formato al mensaje dual unificado en Telegram. Se implementÃ³ un robusto fallback al formato de Gemini clÃ¡sico si DeepSeek falla o no cuenta con saldo suficiente (Error 402), validando el comportamiento con `testAi.js` y `scratch/test_deepseek.js`.
- **[2026-08-02]**: Robustecimiento del parseador en `index.js` (VersiÃ³n 1.8.1). Se actualizaron las expresiones regulares de extracciÃ³n para ignorar los asteriscos de Markdown (`**`) en los encabezados, previniendo fallos si las APIs de IA (Gemini o DeepSeek) devuelven el anÃ¡lisis con formatos resaltados. Se documentÃ³ la necesidad de configurar la variable de entorno `DEEPSEEK_API_KEY` en el panel de control de Railway.
- **[2026-08-04]**: OptimizaciÃ³n de la integraciÃ³n de DeepSeek (VersiÃ³n 1.8.2). Se crearon prompts dedicados (`buildFootballPromptDeepSeek` y `buildBaseballPromptDeepSeek`) para DeepSeek con instrucciones de sÃ­ntesis: anÃ¡lisis de una sola frase (mÃ¡ximo 120 caracteres) y sugerencias directas de apuesta (de 2 a 8 palabras). Se redujo el sesgo de descarte de apuestas ("Evitar apuesta / No recomendada") para incentivar propuestas activas en vivo.
- **[2026-08-04]**: Registro y auditorÃ­a de prompts de IA en Telegram sin costo en Railway (VersiÃ³n 1.8.3). Se implementÃ³ un parÃ¡metro opcional `outContext` en las funciones `generatePrediction` y `generatePredictionDeepSeek` de `aiService.js` para extraer los prompts de entrada sin romper la compatibilidad con las pruebas unitarias. Se modificÃ³ `index.js` para capturar estos prompts y subirlos de forma asÃ­ncrona a un chat o canal secundario de Telegram (`TELEGRAM_PROMPTS_CHAT_ID`) utilizando `bot.sendDocument` con Buffers de texto en memoria. Esto permite al usuario auditar y depurar los prompts completos de forma totalmente gratuita y sin almacenamiento en Railway.
- **[2026-08-04]**: CorrecciÃ³n del mensaje de inicio y remociÃ³n de textos de MLB (VersiÃ³n 1.8.4). Se editÃ³ el comando `/start` en `index.js` para suprimir las menciones de "Bot Multideporte (FÃºtbol + MLB)" y la referencia a las 3 reglas de bÃ©isbol, alineÃ¡ndolo con el alcance de monitoreo actual exclusivo de fÃºtbol.
- **[2026-08-04]**: IntegraciÃ³n de enlaces dinÃ¡micos de bÃºsqueda a Flashscore en alertas de Telegram (VersiÃ³n 1.8.5). Se modificÃ³ la construcciÃ³n de `msgHeader` en `rulesEngine.js` para aÃ±adir enlaces en formato Markdown a bÃºsquedas pre-cargadas en Google con la palabra clave "Flashscore" para el equipo local, el equipo visitante y el partido completo. Esto permite al usuario consultar rÃ¡pidamente estadÃ­sticas e historial desde Telegram en un solo tap sin requerir una API oficial de Flashscore.
- **[2026-08-04]**: Robustecimiento de diagnÃ³stico de auditorÃ­a de prompts (VersiÃ³n 1.8.6). Se implementÃ³ la impresiÃ³n del estado de la variable `TELEGRAM_PROMPTS_CHAT_ID` al arrancar el bot en `index.js`. Se aÃ±adieron logs detallados y explÃ­citos de intento, Ã©xito (con icono âœ…) y error (con icono â�Œ) en el envÃ­o de los documentos de prompts a Telegram para facilitar su auditorÃ­a en el panel de control de Railway.
- **[2026-08-04]**: IdentificaciÃ³n de cuenta de bot en logs (VersiÃ³n 1.8.7). Se agregÃ³ la llamada a `bot.getMe()` en la inicializaciÃ³n de `index.js` para imprimir en consola el nombre de usuario de Telegram del bot que estÃ¡ corriendo actualmente. Esto ayuda a contrastar si la cuenta agregada como administrador del canal coincide exactamente con el token en ejecuciÃ³n.
- **[2026-08-04]**: OptimizaciÃ³n de prompts e integraciÃ³n de enfrentamientos directos H2H (VersiÃ³n 1.8.8). Se excluyÃ³ de forma estricta el partido en vivo en disputa de las listas de los Ãºltimos partidos recientes y enfrentamientos directos. Se implementÃ³ la consulta del H2H a travÃ©s de la API-Football y su correspondiente formateo en los prompts de fÃºtbol de Gemini y DeepSeek. Se controlaron los campos de motivo vacÃ­os configurÃ¡ndolos por defecto como "N/A" para evitar instrucciones truncadas en la IA. Por Ãºltimo, se modificaron los prompts para limitar el anÃ¡lisis a un pÃ¡rrafo conciso de mÃ¡ximo 50 palabras (y mantener la brevedad mÃ¡xima en DeepSeek).
- **[2026-08-04]**: ImplementaciÃ³n de persistencia y auto-suscripciÃ³n ante reinicios de servidor (VersiÃ³n 1.9.0). Se implementÃ³ la funciÃ³n `getPendingPlays` en `financialTracker.js` para extraer las jugadas en estado `PENDING` del archivo `financial_tracker.json`. Se modificÃ³ `index.js` para que al arrancar lea estas jugadas pendientes y reconstruya en memoria el mapa `trackedMatches` para no perder el seguimiento de partidos en curso. Asimismo, se agregÃ³ el ID del usuario principal (`890184744`) por defecto al Set `subscribedChats` en el inicio del bot para asegurar la recepciÃ³n de mensajes tras despliegues sin necesidad de enviar `/start` manualmente.
- **[2026-08-05]**: AuditorÃ­a de rendimiento comparativo del dÃ­a 4 de agosto de 2026 (VersiÃ³n 1.11.1). Se extrajeron de `messages.html` las 18 alertas y veredictos en vivo, contrastando los pronÃ³sticos duales de las IAs mediante bÃºsquedas en la web para obtener los resultados finales y minutos de goles exactos (como en el caso de Dinamo Makhachkala y Alajuelense). Se generÃ³ un dashboard premium interactivo local `resumen_ayer.html` en formato HTML para comparar a Google Gemini (60.0% de efectividad en 9 GREEN / 6 RED / 3 Evitadas) contra DeepSeek (66.67% de efectividad en 12 GREEN / 6 RED / 0 Evitadas) y reportar la caÃ­da de los parlays pre-partido. Se incorporÃ³ un anÃ¡lisis de patrones al HTML identificando a la Regla 3: Sorpresa Tempranera como la regla mÃ¡s recurrente (66.67% de las alertas) y a la Victoria Directa como el mercado mÃ¡s propuesto (14 veces, 64.29% de efectividad combinada), seguido por la lÃ­nea de goles y el prÃ³ximo gol (ambos con 66.67% de efectividad). **Adicionalmente, se creÃ³ el mÃ³dulo `alertsHistory.js` que gestiona el archivo persistente `alerts_history.json` para guardar en vivo todas las alertas y parlays diarios, resolviendo veredictos individuales por IA a travÃ©s de los eventos/stats de la API y de bÃºsquedas web automÃ¡ticas con Gemini (habilitando la nueva funciÃ³n `callGeminiWithGoogleSearch`). Para solucionar la ausencia inicial de registros tras nuevos despliegues en Railway, se integrÃ³ un importador incremental en `alertsHistory.js` que sincroniza mediante fusiÃ³n y cruzado de datos la base de datos con `messages.html` (aÃ±adiendo solo las alertas/parlays no existentes previamente). Por Ãºltimo, se programÃ³ una tarea cron a las 1:00 PM (temporalmente para pruebas de funcionamiento de hoy, originalmente planeada a las 11:00 AM) en `index.js` para generar el HTML `resumen_ayer.html` de forma autÃ³noma y enviarlo por Telegram al canal de auditorÃ­a (`TELEGRAM_PROMPTS_CHAT_ID`).**
- **[2026-08-05]**: ImplementaciÃ³n de reportes de alertas SafeOdds descartadas (VersiÃ³n 1.10.1). Se aÃ±adiÃ³ la recopilaciÃ³n en memoria (`discardedAlertsLog`) de los partidos en espera descartados de la cola de cuotas seguras (SafeOdds) por cambio de marcador o por finalizaciÃ³n de partido. Se implementÃ³ la funciÃ³n asÃ­ncrona `sendDiscardedAlertsReport()` que genera un archivo de texto `.txt` estructurado con la informaciÃ³n de los descartes y lo envÃ­a de forma gratuita mediante Telegram (Buffers de memoria) al canal `TELEGRAM_PROMPTS_CHAT_ID`. Finalmente, se programÃ³ su ejecuciÃ³n al inicio de cada hora en punto (`0 * * * *`) durante el horario de monitoreo activo y se validÃ³ satisfactoriamente con pruebas de integraciÃ³n.
- **[2026-08-05]**: DesactivaciÃ³n del envÃ­o de prompts de IA a Telegram (VersiÃ³n 1.10.2). Se comentÃ³ el bloque de cÃ³digo de envÃ­o en `index.js` para deshabilitar de forma definitiva la subida de los archivos de texto (`prompt_gemini_*.txt` y `prompt_deepseek_*.txt`) al canal secundario de Telegram `TELEGRAM_PROMPTS_CHAT_ID` a peticiÃ³n del usuario. Las variables asociadas de contexto para pruebas se mantienen sin alterar y el resto de la lÃ³gica de alertas en vivo permanece activa.
- **[2026-07-21]**: Creación de la bitácora y análisis inicial de requerimientos.
- **[2026-07-21]**: Implementación del código base en Node.js (`apiClient.js`, `rulesEngine.js`, `index.js`). Pruebas de simulación exitosas.
- **[2026-07-21]**: Creación de `config.js` para filtrado por ligas principales. Implementación de Reglas 5, 6 y 7, junto con `evaluateAlertResults` (GREEN/RED).
- **[2026-07-22]**: Creación de los módulos de MLB Béisbol (`baseballApiClient.js`, `baseballRulesEngine.js`, `testBaseball.js`). Integración multideporte en `index.js` y desplegado en Railway.
- **[2026-07-22]**: Resolución de Error HTTP 429 (Rate Limit de API-Sports). Se implementó un sistema de caché negativo (`NO_ODDS`), filtrado de partidos en vivo para evitar consultas redundantes de partidos finalizados y cooldown automático de 60 segundos ante status 429 en `apiClient.js` y `baseballApiClient.js`. Reducción masiva de peticiones API de ~50+/min a ~2-4/min. Pruebas de integración exitosas.
- **[2026-07-22]**: Integración de Recomendaciones de Apuestas y Cuota Objetivo `@1.60 o más`. Se actualizaron los motores de reglas (`rulesEngine.js` y `baseballRulesEngine.js`) para incluir la recomendación directa del mercado a operar (ej: Doble Chance, Over 0.5 2HT, Córneres Totales, Hándicap) junto con la cuota objetivo sugerida `@1.60 o más`. Pruebas en `test.js` y `testBaseball.js verificadas.
- **[2026-07-22]**: Restricción de Horario de Monitoreo (7 AM - 9 PM Hora Centro México). Se configuró `America/Mexico_City` y `isWithinActiveHours()` en `config.js` e `index.js` para ejecutar el polling por cron únicamente de 07:00 a 21:00 hrs CST/CDT, previniendo el consumo innecesario de peticiones API durante la noche/madrugada.
- **[2026-07-22]**: Despliegue Exitoso a Producción (`main`). Se enviaron todos los cambios y mejoras (solución Rate Limit 429, cuotas objetivo @1.60 y restricción de horario 7 AM - 9 PM CDMX) al repositorio GitHub, activando el despliegue automático en Railway.
- **[2026-07-22]**: Investigación sobre Alertas de MLB. Se detectó que la API de béisbol (`v1.baseball.api-sports.io`) con el plan gratuito ("Free Plan") bloquea el acceso a la temporada actual devolviendo el error: `Free plans do not have access to this season, try from 2022 to 2024.` Adicionalmente, se identificó que el horario restrictivo de monitoreo (7 AM - 9 PM CDMX) corta el seguimiento de partidos nocturnos de la MLB que finalizan más tarde.
- **[2026-07-23]**: Integración de Inteligencia Artificial (Gemini) para análisis y recomendaciones dinámicas. Se creó el módulo `aiService.js` para consumir la API de REST de Gemini con Axios de forma nativa. Se implementó un sistema de rotación de múltiples claves y fallback de modelo (`gemini-flash-latest` -> `gemini-2.5-flash`). Se diseñaron prompts irreverentes e informales en español para fútbol y MLB. Para resolver el truncado por consumo de tokens del razonamiento interno (Thinking), se elevó el límite a `maxOutputTokens: 4000` y el timeout a 20 segundos. Se integró la llamada en `index.js` para fútbol y béisbol reemplazando dinámicamente las recomendaciones fijas con fallbacks tolerantes a errores.
- **[2026-07-23]**: Enriquecimiento y formateo de estadísticas y eventos para la IA. Se implementó un sistema de consulta dinámica on-demand para fútbol en `index.js` que descarga la información detallada de estadísticas y eventos al activarse cualquier alerta, evitando consumos innecesarios en el polling pasivo. Se crearon funciones de formateo para fútbol (`formatFootballStats` y `formatFootballEvents`) y para béisbol (`formatBaseballStats`) que traducen las estructuras JSON de API-Sports en resúmenes textuales de alta legibilidad y bajo consumo de tokens. Se ajustaron los prompts de Gemini en `aiService.js` para obligar al modelo a usar y referenciar estas métricas reales (posesión, tiros, córneres, hits, errores, desglose por entradas) en sus análisis jocosos e irreverentes. Pruebas integradas en `testAi.js` completadas exitosamente.
- **[2026-07-24]**: Análisis Histórico de 5 Partidos e Inteligencia Artificial en Tono Formal. Se implementó la función `getTeamLastMatches` en `apiClient.js` para consultar los últimos 5 partidos jugados de cualquier equipo en fútbol. Se integró esta lógica en `index.js` para consultar dinámicamente dichos partidos (con delays entre peticiones para control de tasa) al saltar una alerta, adjuntándolos a `matchData` para alimentar el prompt. Se agregaron y actualizaron los prompts en `aiService.js` para cambiar el estilo de análisis a un formato formal, objetivo y técnico (para fútbol y béisbol MLB), eliminando el tono jocoso e irreverente original. Se actualizó el archivo de pruebas `testAi.js` agregando los mock de partidos históricos de fútbol, validando exitosamente el comportamiento formal y robustecido en `testAi_output.txt`. **Adicionalmente, se corrigió un sesgo de recomendación por defecto en `index.js` mediante la extracción de `cleanRuleDetails`, eliminando las recomendaciones estáticas de las alertas de fútbol y béisbol para garantizar que la IA realice una evaluación táctica e independiente basada exclusivamente en datos estadísticos e históricos reales.**
- **[2026-07-24]**: Optimización de Consumo y Throttling de API-Sports (Opción B). Se resolvió el sobreconsumo que agotó las 7,500 peticiones del plan PRO. Se optimizó `needsEvents` en `rulesEngine.js` para que solo determine consultar la API si el partido cumple las pre-condiciones del marcador (empate o ventaja del underdog para Tarjeta Roja). También se implementó un sistema de Throttling con cooldown de 4 minutos y almacenamiento en caché en memoria (`eventsCache` y `statsCache`) en `index.js`, garantizando la reducción del consumo y la reutilización segura de datos recientes en lugar de enviar arreglos vacíos. Se integró un mecanismo automático de limpieza de cachés al finalizar partidos para evitar fugas de memoria.
- **[2026-07-25]**: Integración del Sistema de Parlays (Pre-partido y En Vivo). Se modificaron los prompts en `aiService.js` para exigir la respuesta con un nivel de confianza estimado (`🔥 Confianza Estimada: [Porcentaje]%`). Se implementó `generateDailyParlay` para armar combinadas de alta certeza utilizando datos de momios e historial reciente de equipos seleccionados. En `index.js`, se programaron tres tareas cron (8:30 AM, 9:30 AM y 10:30 AM) para publicar el Parlay del Día con partidos del día no iniciados de ligas importantes. Asimismo, se implementó un sistema de cola en memoria (`liveAlertsQueue` con expiración de 40 minutos) que detecta y combina automáticamente 2 o más alertas en vivo de alta confianza (>= 85%) en un Parlay en Vivo dinámico de Telegram. Se actualizaron las pruebas en `testAi.js` y se verificó el correcto funcionamiento. **Adicionalmente, se actualizó la versión del bot a v1.5.0 en package.json y se integró de forma dinámica en el comando /start de Telegram en index.js para facilitar la verificación visual de la versión instalada. Finalmente, se solucionó un error lógico crítico de la IA (alucinación de momios en vivo) mediante la integración de una regla estricta de realismo en los prompts de aiService.js, evitando la recomendación de mercados casi resueltos con cuotas altas (ej: Doble Oportunidad a favor del equipo que ya va ganando) y forzando a Gemini a proponer mercados lógicos de reacción (1X/X2 del que va perdiendo, ML, próximo gol, Over de goles).**
- **[2026-07-25]**: Corrección del Sistema de Veredictos GREEN/RED con IA. Se solucionó el fallo donde las alertas con predicciones personalizadas de IA se calificaban erróneamente usando la validación estática original. Se implementó la función `evaluatePredictionOutcome` en `aiService.js` para consultar a Gemini y evaluar de forma dinámica y contextual la recomendación real enviada al canal basada en el marcador final, estadísticas y eventos. En `index.js`, se modificó el guardado en caché de alertas para registrar la propiedad `aiRecommendation` y se hizo asíncrona la resolución de veredictos post-partido (`evaluateAlertResults` y `evaluateBaseballAlertResults`). Se incluyó un fallback automático y transparente hacia el motor estático cuando no hay recomendación de IA o falla la API. Se actualizaron las pruebas en `test.js`, `testBaseball.js` y se validó en `testAiEvaluation.js` tanto para veredictos RED como GREEN.
- **[2026-07-26]**: Análisis de rendimiento de estrategias basado en el archivo de mensajes de Telegram (`messages.html`). Se implementó un script de parseo secuencial (`analyze.js`) que extrajo 182 veredictos históricos totales (con un 79.12% de aciertos global) y 79 veredictos del día anterior, 25 de julio de 2026 (con un 82.28% de aciertos). Se creó un reporte interactivo premium en HTML (`reporte_rendimiento.html`) con un diseño moderno responsivo en modo oscuro que presenta gráficos y desgloses de rendimiento por estrategia, clasificando cuáles funcionan con alto acierto (como "Tarjeta Roja Estratégica" y "Partido Caliente / Tarjetas") y cuáles requieren revisión crítica (como "HT Comeback Favorito").
- **[2026-07-26]**: Integración de Descarte Inteligente de Apuestas por IA. Se actualizaron los prompts en [aiService.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/aiService.js) para instruir a la IA a que recomiende omitir apuestas de alto riesgo con la frase "Evitar apuesta / No recomendada". En [rulesEngine.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/rulesEngine.js) and [baseballRulesEngine.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/baseballRulesEngine.js), se modificó la resolución de veredictos post-partido para interceptar este descarte de la IA, emitiendo un reporte neutral `⚪ APUESTA EVITADA` en Telegram con una comparativa sobre si la sugerencia evitó un fallo (RED) o priorizó la precaución sobre un acierto (GREEN), y marcándolo en metadatos como `isOmitted: true` para fines de medición de rendimiento real. Se validaron los cambios exitosamente en [test_omission.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/test_omission.js).
- **[2026-07-26]**: Implementación del **Sistema SafeOdds** de retención y monitoreo de alertas en tiempo real. Se integró una cola en memoria `pendingAlertsQueue` y soporte para consultar el endpoint `/odds/live` de API-Football cada minuto. Se implementó la lógica de mapeo inteligente de recomendaciones a cuotas activas (`getLiveOddForRecommendation`) y un estimador matemático de cuotas en vivo como fallback secundario por tiempo de espera (`waitMinutes`) ante la ausencia de datos en vivo de la API. Se programó la validación minuto a minuto en `processPendingAlerts()`, de modo que si hay cambio de marcador antes de alcanzar la cuota objetivo se cancela la alerta automáticamente, y de lo contrario se envía al canal de Telegram al alcanzar el momio mínimo sugerido (ej. @1.60+). Se creó y validó el script de simulación `testSafeOdds.js` aprobando los escenarios de disparo por cuota, disparo por tiempo estimado y cancelación por gol.
- **[2026-07-26]**: Rediseño estético y de formato premium para las notificaciones de Telegram (fútbol y béisbol MLB). Se simplificaron los encabezados de los partidos compactando el tiempo, marcador e inning en una sola línea horizontal. Se envolvieron los datos clave del evento con separadores de línea gruesa (`━━━━━━━━━━━━━━━━━━━━━━━━━━━━`). Se implementó un reformateador de la predicción de IA en `index.js` que desglosa la respuesta de Gemini en una estructura elegante con iconos específicos y negritas uniformes. Se robustecieron las expresiones regulares de extracción en `extractConfidence` y `handleLiveParlayQueue` para soportar formatos ricos con asteriscos y negritas de Markdown. Se validó visualmente el resultado con el script de pruebas `testRender.js`.
- **[2026-07-26]**: Desactivación y remoción del monitoreo de béisbol (MLB). Se comentaron las importaciones de béisbol en `index.js`, se borraron las variables de caché asociadas y se eliminaron por completo las funciones de análisis `checkBaseballMatches` y `checkFinishedBaseballMatches`. También se removieron sus ejecuciones en el programador cron minuto a minuto y en la inicialización de arranque, orientando el bot actual exclusivamente al análisis de fútbol.
- **[2026-07-26]**: Corrección en el mapeo de cuotas en vivo de fútbol en `getLiveOddForRecommendation()`. Se introdujo la discriminación estricta de palabras clave para que el bot ignore búsquedas de cuotas si la recomendación es de Tarjetas o Córneres (retornando `null` de forma segura para usar el estimador de tiempo/fallback en su lugar). Para recomendaciones de Goles, se implementó la extracción automática del número de línea de goles y su comparación exacta con la propiedad `handicap` en los valores de la API, evitando que el bot lea cuotas de líneas de goles muy avanzadas de forma errónea.
- **[2026-07-26]**: Ajuste en el SafeOdds System. Se fijó la cuota objetivo (`targetOdd`) en `1.60` de forma constante para todas las alertas. Esto permite que el bot envíe la alerta al canal de Telegram inmediatamente cuando el momio en vivo alcance o supere `@1.60`, en lugar de esperar a que la cuota suba hasta el momio sugerido de la IA (ej. @1.95), previniendo que un gol temprano antes de alcanzar la cuota alta cancele la alerta.
- **[2026-07-26]**: Corrección Crítica en la Evaluación de Veredictos (Rate Limit Exhaustion). Se resolvió el fallo de "veredicto no enviado a partidos terminados". Se descubrió que partidos cancelados, suspendidos o aplazados (`CANC`, `PST`, `ABD`) se quedaban estancados en memoria sin ser eliminados, causando que el bot consultara repetitivamente la API cada minuto y agotara su cuota de peticiones. Se implementó una eliminación dinámica de partidos con estatus problemáticos. Adicionalmente, se protegió la evaluación iterativa (`checkFinishedMatches`) con bloques `try...catch` para evitar que una excepción por falta de momios (TypeError) en una alerta (como *Sorpresa tempranera*) abortara el proceso de evaluación de todos los demás partidos. Finalmente, se excluyó de evaluación toda alerta que el sistema SafeOdds nunca alcanzó a enviar al canal de Telegram.
- **[2026-07-27]**: Optimización Crítica de Latencia y Velocidad en Envío de Alertas. Se solucionó el retraso percibido en el envío de alertas de fútbol. En `index.js`, se paralelizó la obtención asíncrona de estadísticas, eventos y partidos recientes usando `Promise.all` para evitar el bloqueo secuencial. Además, se reescribió la lógica de integración de *SafeOdds*: si el mercado recomendado no es monitorizable (como tarjetas o córneres) o si el partido no tiene cobertura de cuotas en vivo en la API, la alerta se envía de inmediato, en lugar de encolarla artificialmente. Para alertas que sí se encolan esperando alcanzar una cuota de `@1.60`, se fijó un límite máximo de retención de 8 minutos para el estimador de tiempo. Finalmente, se eliminó el `cron` de 1 minuto para el monitoreo de partidos (`checkMatches`) y se reemplazó por un bucle asíncrono dinámico protegido que sondea los partidos cada **30 segundos** en horario activo. Se incrementó la versión a `1.5.6`.
- **[2026-07-28]**: Análisis retrospectivo de alertas y veredictos en `messages.html` (desde el 26 de julio a las 5:00 PM). Se desarrolló un script en Node.js (`scratch/analyze_messages.js`) que extrajo 38 alertas de reglas y 32 veredictos, logrando cruzar con éxito 31 de ellas. Se ejecutó una simulación financiera partiendo de un capital inicial de $5,000 y apostando $100 a cada recomendación (excluyendo alertas no recomendadas por la IA), finalizando con un balance de $4,900. Se construyó una interfaz interactiva premium `reporte_rendimiento.html` con diseño oscuro y acordeones interactivos CSS para desplegar los partidos al hacer clic en cada regla.
- **[2026-07-28]**: Ampliación del análisis a la totalidad del historial de mensajería en `messages.html`. Se actualizó el script `analyze_messages.js` para suprimir las restricciones de fecha y procesar el 100% de los datos (273 alertas totales y 234 veredictos). Se reestructuró el script para automatizar la generación y regeneración dinámica del reporte HTML `reporte_rendimiento.html`, inyectando programáticamente todas las estadísticas reales, acordeones de reglas con sus correspondientes tablas de partidos (220 cruces exitosos) y la simulación del balance de transacciones. La simulación global arrojó un balance final de $11,206.00 partiendo de $5,000 iniciales, con una efectividad global del 75.12% en las recomendaciones recomendadas.
- **[2026-07-29]**: Integración del Sistema de Control Financiero Diario y Veredictos Web Autónomos (Versión 1.6.0). Se creó el módulo `financialTracker.js` y el archivo `financial_tracker.json` para llevar un balance persistente de las jugadas del bot, iniciando con un capital base of $5,000 MXN y apuestas de $250 MXN (5%) por recomendación (excluyendo descartes "Evitar apuesta"). Se interceptaron los 4 puntos de envío de alertas en `index.js` y la evaluación de veredictos post-partido en `checkFinishedMatches` para registrar y actualizar jugadas en tiempo real. En `aiService.js`, se implementó el método `resolveVerdictViaWeb` que utiliza Gemini 2.5 Flash con Google Search Grounding (`google_search`) para resolver veredictos de partidos pasados a través de búsquedas en la web cuando falten datos en la API de fútbol. Por último, se programó un cron a las 7:35 AM (Hora Centro México) en `index.js` para depurar jugadas pendientes (API + Web) y enviar el reporte financiero diario de efectividad y balance de ayer y del acumulado semanal desde el domingo.
- **[2026-07-30]**: Integración del Contexto de Competición y Marcador Global en la IA (Versión 1.7.0). Se modificó `index.js` para extraer y enviar las propiedades `leagueName` y `leagueRound` dentro de `matchData` al solicitar predicciones a la IA. Se actualizaron los prompts en `aiService.js` para fútbol y béisbol, instruyendo a Gemini a clasificar explícitamente el partido en tres escenarios tácticos: eliminatoria de ida y vuelta (calculando el marcador global lógicamente a partir del partido previo del historial recente), localía neutral (ignorando la ventaja típica de local), y torneos o ligas normales (evaluando la localía estándar y la urgencia de sumar los 3 puntos). Por último, se modificaron los mocks de prueba en `testAi.js` y se añadió un nuevo caso de prueba para una eliminatoria de vuelta de la UEFA Champions League, validando con éxito (en `testAi_output.txt`) que la IA deduce el marcador global de 3-2 en contra del Real Madrid y emite la recomendación de buscar el gol de manera táctica, mientras que en partidos normales de liga evalúa la urgencia estándar sin alucinar marcadores acumulados.
- **[2026-07-30]**: Re-despliegue correctivo y actualización a v1.7.1 por error detectado en el despliegue automático de Railway, forzando un nuevo build a través de GitHub tras corroborar que las pruebas locales de sintaxis y respuesta de IA pasan satisfactoriamente.
- **[2026-08-01]**: Actualización de credenciales de Gemini en el archivo `.env`. Se reemplazaron las claves API de la cuenta con facturación por las nuevas claves gratuitas proporcionadas por el usuario para migrar al plan sin costos (Free Tier).
- **[2026-08-01]**: Integración de DeepSeek para análisis predictivo dual en vivo. Se configuró la variable de entorno `DEEPSEEK_API_KEY` en el archivo `.env` y se crearon las funciones `callDeepSeekWithRotation` y `generatePredictionDeepSeek` en `aiService.js` con soporte para modelos `deepseek-v4-flash` and `deepseek-v4-pro`. Se adaptó `index.js` para realizar consultas paralelas protegidas a ambos proveedores de IA y dar formato al mensaje dual unificado en Telegram. Se implementó un robusto fallback al formato de Gemini clásico si DeepSeek falla o no cuenta con saldo suficiente (Error 402), validando el comportamiento con `testAi.js` y `scratch/test_deepseek.js`.
- **[2026-08-02]**: Robustecimiento del parseador en `index.js` (Versión 1.8.1). Se actualizaron las expresiones regulares de extracción para ignorar los asteriscos de Markdown (`**`) en los encabezados, previniendo fallos si las APIs de IA (Gemini o DeepSeek) devuelven el análisis con formatos resaltados. Se documentó la necesidad de configurar la variable de entorno `DEEPSEEK_API_KEY` en el panel de control de Railway.
- **[2026-08-04]**: Optimización de la integración de DeepSeek (Versión 1.8.2). Se crearon prompts dedicados (`buildFootballPromptDeepSeek` y `buildBaseballPromptDeepSeek`) para DeepSeek con instrucciones de síntesis: análisis de una sola frase (máximo 120 caracteres) y sugerencias directas de apuesta (de 2 a 8 palabras). Se redujo el sesgo de descarte de apuestas ("Evitar apuesta / No recomendada") para incentivar propuestas activas en vivo.
- **[2026-08-04]**: Registro y auditoría de prompts de IA en Telegram sin costo en Railway (Versión 1.8.3). Se implementó un parámetro opcional `outContext` en las funciones `generatePrediction` y `generatePredictionDeepSeek` de `aiService.js` para extraer los prompts de entrada sin romper la compatibilidad con las pruebas unitarias. Se modificó `index.js` para capturar estos prompts y subirlos de forma asíncrona a un chat o canal secundario de Telegram (`TELEGRAM_PROMPTS_CHAT_ID`) utilizando `bot.sendDocument` con Buffers de texto en memoria. Esto permite al usuario auditar y depurar los prompts completos de forma totalmente gratuita y sin almacenamiento en Railway.
- **[2026-08-04]**: Corrección del mensaje de inicio y remoción de textos de MLB (Versión 1.8.4). Se editó el comando `/start` en `index.js` para suprimir las menciones de "Bot Multideporte (Fútbol + MLB)" y la referencia a las 3 reglas de béisbol, alineándolo con el alcance de monitoreo actual exclusivo de fútbol.
- **[2026-08-04]**: Integración de enlaces dinámicos de búsqueda a Flashscore en alertas de Telegram (Versión 1.8.5). Se modificó la construcción de `msgHeader` en `rulesEngine.js` para añadir enlaces en formato Markdown a búsquedas pre-cargadas en Google con la palabra clave "Flashscore" para el equipo local, el equipo visitante y el partido completo. Esto permite al usuario consultar rápidamente estadísticas e historial desde Telegram en un solo tap sin requerir una API oficial de Flashscore.
- **[2026-08-04]**: Robustecimiento de diagnóstico de auditoría de prompts (Versión 1.8.6). Se implementó la impresión del estado de la variable `TELEGRAM_PROMPTS_CHAT_ID` al arrancar el bot en `index.js`. Se añadieron logs detallados y explícitos de intento, éxito (con icono ✅) y error (con icono ❌) en el envío de los documentos de prompts a Telegram para facilitar su auditoría en el panel de control de Railway.
- **[2026-08-04]**: Identificación de cuenta de bot en logs (Versión 1.8.7). Se agregó la llamada a `bot.getMe()` en la inicialización de `index.js` para imprimir en consola el nombre de usuario de Telegram del bot que está corriendo actualmente. Esto ayuda a contrastar si la cuenta agregada como administrador del canal coincide exactamente con el token en ejecución.
- **[2026-08-04]**: Optimización de prompts e integración de enfrentamientos directos H2H (Versión 1.8.8). Se excluyó de forma estricta el partido en vivo en disputa de las listas de los últimos partidos recientes y enfrentamientos directos. Se implementó la consulta del H2H a través de la API-Football y su correspondiente formateo en los prompts de fútbol de Gemini y DeepSeek. Se controlaron los campos de motivo vacíos configurándolos por defecto como "N/A" para evitar instrucciones truncadas en la IA. Por último, se modificaron los prompts para limitar el análisis a un párrafo conciso de máximo 50 palabras (y mantener la brevedad máxima en DeepSeek).
- **[2026-08-04]**: Implementación de persistencia y auto-suscripción ante reinicios de servidor (Versión 1.9.0). Se implementó la función `getPendingPlays` en `financialTracker.js` para extraer las jugadas en estado `PENDING` del archivo `financial_tracker.json`. Se modificó `index.js` para que al arrancar lea estas jugadas pendientes y reconstruya en memoria el mapa `trackedMatches` para no perder el seguimiento de partidos en curso. Asimismo, se agregó el ID del usuario principal (`890184744`) por defecto al Set `subscribedChats` en el inicio del bot para asegurar la recepción de mensajes tras despliegues sin necesidad de enviar `/start` manualmente.
- **[2026-08-05]**: Auditoría de rendimiento comparativo del día 4 de agosto de 2026 (Versión 1.11.1). Se extrajeron de `messages.html` las 18 alertas y veredictos en vivo, contrastando los pronósticos duales de las IAs mediante búsquedas en la web para obtener los resultados finales y minutos de goles exactos (como en el caso de Dinamo Makhachkala y Alajuelense). Se generó un dashboard premium interactivo local `resumen_ayer.html` en formato HTML para comparar a Google Gemini (60.0% de efectividad en 9 GREEN / 6 RED / 3 Evitadas) contra DeepSeek (66.67% de efectividad en 12 GREEN / 6 RED / 0 Evitadas) y reportar la caída de los parlays pre-partido. Se incorporó un análisis de patrones al HTML identificando a la Regla 3: Sorpresa Tempranera como la regla más recurrente (66.67% de las alertas) y a la Victoria Directa como el mercado más propuesto (14 veces, 64.29% de efectividad combinada), seguido por la línea de goles y el próximo gol (ambos con 66.67% de efectividad). **Adicionalmente, se creó el módulo `alertsHistory.js` que gestiona el archivo persistente `alerts_history.json` para guardar en vivo todas las alertas y parlays diarios, resolviendo veredictos individuales por IA a través de los eventos/stats de la API y de búsquedas web automáticas con Gemini (habilitando la nueva función `callGeminiWithGoogleSearch`). Para solucionar la ausencia inicial de registros tras nuevos despliegues en Railway, se integró un importador incremental en `alertsHistory.js` que sincroniza mediante fusión y cruzado de datos la base de datos con `messages.html` (añadiendo solo las alertas/parlays no existentes previamente). Por último, se programó una tarea cron a las 1:00 PM (temporalmente para pruebas de funcionamiento de hoy, originalmente planeada a las 11:00 AM) en `index.js` para generar el HTML `resumen_ayer.html` de forma autónoma y enviarlo por Telegram al canal de auditoría (`TELEGRAM_PROMPTS_CHAT_ID`).**
- **[2026-08-05]**: Implementación de reportes de alertas SafeOdds descartadas (Versión 1.10.1). Se añadió la recopilación en memoria (`discardedAlertsLog`) de los partidos en espera descartados de la cola de cuotas seguras (SafeOdds) por cambio de marcador o por finalización de partido. Se implementó la función asíncrona `sendDiscardedAlertsReport()` que genera un archivo de texto `.txt` estructurado con la información de los descartes y lo envía de forma gratuita mediante Telegram (Buffers de memoria) al canal `TELEGRAM_PROMPTS_CHAT_ID`. Finalmente, se programó su ejecución al inicio de cada hora en punto (`0 * * * *`) durante el horario de monitoreo activo y se validó satisfactoriamente con pruebas de integración.
- **[2026-08-05]**: Desactivación del envío de prompts de IA a Telegram (Versión 1.10.2). Se comentó el bloque de código de envío en `index.js` para deshabilitar de forma definitiva la subida de los archivos de texto (`prompt_gemini_*.txt` y `prompt_deepseek_*.txt`) al canal secundario de Telegram `TELEGRAM_PROMPTS_CHAT_ID` a petición del usuario. Las variables asociadas de contexto para pruebas se mantienen sin alterar y el resto de la lógica de alertas en vivo permanece activa.
- **[2026-08-05]**: Creación de la versión 2.0.0. Se restablece el código del bot de alertas al estado exacto de la versión v1.9.0 (commit e8c0734) y se aumenta la versión en package.json a v2.0.0, desactivando los módulos de historial de alertas (alertsHistory), reportes horarios de SafeOdds descartadas y reportes diarios automatizados Gemini vs DeepSeek.
- **[2026-08-05]**: Robustecimiento y visibilidad de alertas en vivo (Versión 2.0.1). Se implementó la función auxiliar `sendSafeMarkdownMessage` para gestionar el envío de mensajes con formato `Markdown` del bot de Telegram de forma robusta; en caso de que Telegram arroje un error por problemas de parseo debido a caracteres especiales en la respuesta de la IA, el bot reintenta automáticamente el envío en texto plano sin `parse_mode` para asegurar la entrega de la alerta en vivo. Además, se desarrolló la función `logSafeOddsEvent` para enviar notificaciones en vivo al canal de auditoría `TELEGRAM_PROMPTS_CHAT_ID` cada vez que una alerta de fútbol sea encolada en `pendingAlertsQueue` o descartada de la cola (por cambio de marcador o finalización de partido), dándole visibilidad total al usuario sobre el flujo del motor de cuotas seguras (SafeOdds).
- **[2026-08-06]**: Auditoría completa de efectividad del bot y análisis de IAs basado en `messages.html`. Se creó un script de análisis (`scratch/analizar.js`) para parsear las 49 alertas y 35 veredictos registrados en el chat. Se completó el análisis consultando en Internet el resultado de los 14 partidos que no contaban con veredicto en el chat. De las 49 alertas procesadas (28 de Sorpresa Tempranera, 8 de Tarjeta Roja, 7 de Partido Caliente, 3 de Favorito Sufre, 2 de Asedio Intenso y 1 de Remontada Potencial), se consolidaron las siguientes estadísticas:
  - **Google Gemini**: Recomendó operar 44 apuestas, obteniendo 28 aciertos (GREEN) y 16 fallos (RED) para una efectividad del **63.6%**. En las 5 alertas que recomendó evitar (Brønshøj, Viby, Brann y dos en el HTML), se previno la pérdida de capital en un 60.0% de los casos (3/5).
  - **DeepSeek**: Recomendó operar en el 100% de las 49 alertas sin emitir ningún descarte de "Evitar apuesta". Obtuvo 32 aciertos (GREEN) y 17 fallos (RED) para una efectividad del **65.3%**.
  - **Estrategia Más Frecuente**: La *Regla 3: Sorpresa Tempranera* representó el **57.1%** del total de las alertas emitidas por el bot (28 de 49).
  - **Generación de Reporte Visual**: Se implementó el script `scratch/generar_reporte.js` que consolida todo este análisis e inyecta dinámicamente los 10 marcadores externos de la web en una interfaz interactiva premium en modo oscuro llamada `reporte_auditoria_messages.html` en la raíz del proyecto.
- **[2026-08-07]**: Despliegue de Optimización Core de Reglas de Fútbol (Versión 2.1.0). Se implementó un robusto **Filtro de Consenso IA** en `index.js` que intercepta y aborta silenciosamente las alertas si Gemini devuelve una confianza menor al 40% o recomienda "Evitar apuesta". En `rulesEngine.js`, se rediseñó el motor para integrar estadísticas avanzadas (posesión, tiros a puerta, ataques peligrosos) aplicando el modelo de Degradación Elegante (Graceful Fallback) en caso de que ligas menores no proporcionen stats. Las modificaciones específicas incluyen:
  - Regla 1 (Tarjeta Roja): Filtro de impulso (posesión > 55%).
  - Regla 8 (Nueva): Favorito Domina HT, reemplaza el viejo "Favorito Sufre" disparándose con cualquier empate al medio tiempo si el favorito registra > 60% posesión y alta generación ofensiva.
  - Regla 3 (Sorpresa): Acortada al minuto 41 y abortada si el favorito está masacrando al underdog.
  - Regla 4 (Asedio): Exigencia de tiros a puerta (>= 4) y ataques peligrosos.
-   * * [ 2 0 2 6 - 0 8 - 0 7 ] * * :   C o r r e c c i ó n   d e   e r r o r   d e   s i n t a x i s   e n   R e g l a   1   ( V e r s i ó n   2 . 2 . 1 ) .   S e   a r r e g l ó   u n   e r r o r   c r í t i c o   ( \ S y n t a x E r r o r :   U n e x p e c t e d   e n d   o f   i n p u t \ )   c a u s a d o   p o r   u n a   l l a v e   d e   c i e r r e   f a l t a n t e   e n   e l   b l o q u e   d e   l a   R e g l a   1   e n   \ 
 u l e s E n g i n e . j s \ ,   e l   c u a l   p r o v o c a b a   e l   c o l a p s o   d e l   b o t   e n   d e s p l i e g u e .   S e   i n c r e m e n t ó   l a   v e r s i ó n   a   v 2 . 2 . 1   p a r a   r e f l e j a r   e l   h o t f i x . 
 
 
- **[2026-08-08]**: Auditoría completa de rendimiento de `messages.html` del día 7 de agosto de 2026. Se desarrolló un script de procesamiento `scratch/analyze_yesterday.js` que parseó 22 alertas y 22 veredictos post-partido registrados en el chat. Se determinó un 50.0% de aciertos global (11 GREEN / 11 RED). En la comparativa de IAs, DeepSeek logró un 50.0% de aciertos (11 GREEN / 11 RED en 22 apuestas) mientras que Google Gemini logró un 47.6% (10 GREEN / 11 RED en 21 apuestas recomendadas, más 1 evitada). La Regla 1 (Tarjeta Roja Estratégica) mantuvo una efectividad impecable del 100% (3/3 GREEN), mientras que la Regla 3 (Sorpresa Tempranera) registró 8 GREEN y 10 RED. Se registraron 3 parlays del día.
- **[2026-08-08]**: Generación de Dashboard Interactivo Premium `resumen_ayer.html`. Se construyó el script `scratch/generar_resumen_ayer.js` que compila los resultados de auditoría de la jornada del 7 de agosto de 2026 en una interfaz web responsiva en modo oscuro con diseño Glassmorphism, tipografía Google Fonts (Inter/Outfit) e iconos Lucide. El dashboard incluye tarjetas KPI de efectividad (50.0% GREEN / 50.0% RED), comparativa visual en barras de progreso entre Google Gemini (47.6%) y DeepSeek (50.0%), filtro dinámico por estado de veredicto (Todas, GREEN, RED, Parlays) y barra de búsqueda en tiempo real.
- **[2026-08-08]**: Implementación de Veredictos Duales Post-Partido para Google Gemini y DeepSeek (Versión 2.3.0). Se modificó `index.js` para almacenar en los metadatos de la alerta tanto la recomendación de Google Gemini (`geminiRecommendation`) como la de DeepSeek (`deepseekRecommendation`). Se reestructuró la función `evaluateAlertResults` en `rulesEngine.js` para realizar la evaluación independiente por IA de ambas predicciones al finalizar el partido. Se rediseñó la plantilla del veredicto post-partido en Telegram (`VEREDICTO POST-PARTIDO - DUAL`) para mostrar explícitamente el resultado individual (GREEN 🟩 / RED 🟥) de cada modelo de IA junto con su justificación contextual, evitando veredictos GREEN falsos o engañosos basados en fallbacks estáticos de reglas contradictorias. Pruebas de integración superadas exitosamente en `scratch/test_dual_verdict.js`.
- **[2026-08-10]**: Definición de Arquitectura de Monetización y Automatización de Contenidos (Sesión /grill-me):
  - Definición de oferta comercial: Combo VIP ($25-$30 USD/mes), Módulo En Vivo individual ($15 USD/mes) y Pase Semanal ($10 USD).
  - Automatización de accesos y pagos recurrentes mediante InviteMember/Tribute en Telegram.
  - **Motor de Generación y Publicación Automática de Videos (100% Autónomo)**:
    - Generación programática de videos 9:16 (Puppeteer + FFmpeg + Text-To-Speech) al confirmar un acierto GREEN 🟢 o boletín destacado.
    - Publicación automática multicanal en Instagram Reels (Meta Graph API), YouTube Shorts (YouTube API v3) y TikTok (Posting API / Webhooks).
- **[2026-08-10]**: Reporte de Rendimiento Histórico Completo basado en `messages.html` (Versión 2.4.0). Se creó el script de extracción y análisis `scratch/generar_reporte_messages.js` que procesó 118 alertas y 100 veredictos (incluyendo Veredictos Duales) acumulados entre el 4 y el 10 de agosto de 2026. Se generó la aplicación web de reporte interactivo `reporte_messages.html` en la raíz del proyecto, la cual incluye:
  - **Métricas Generales**: Dashboard interactivo con 118 alertas totales, 84 veredictos evaluados (66.7% Win Rate directo) y gráficos visuales de tendencia con Chart.js.
  - **Separado por Días**: Pestañas de filtrado individual para cada día del rango (04.08, 05.08, 06.08, 07.08, 08.08, 09.08, 10.08) con marcadores, ligas, minutos y recomendaciones individuales.
  - **Análisis por IA**: Comparativa detallada entre Google Gemini (65.8% acierto) y DeepSeek (67.1% acierto), matriz de consenso dual y métricas de trampas/apuestas evitadas.
  - **Por Tipo de Regla**: Desglose de rendimiento para las 8 reglas activas (Sorpresa Tempranera, Roja Estratégica, Asedio Favorito, Partido Caliente, Favorito Domina HT, etc.).
  - **Efectividad y Confianza**: Análisis de rendimiento según rangos de confianza de la IA (Alta >=70%, Media 50-69%, Baja <50%).
  - **Simulador Financiero ($100 MXN)**: Pestaña interactiva de cálculo de rendimiento con flat stake de $100 pesos por apuesta bajo cuotas de @1.65, @1.70 y @1.80.
  - **Por Tipo de Apuesta (Mercados)**: Agrupación interactiva por mercados (Victoria Directa ML, Próximo Gol, Línea de Goles Over/Under, Doble Oportunidad, Tarjetas, Córneres) con comparativa Gemini vs DeepSeek.
  - **Resolución al 100% de Veredictos Pendientes (Regla 3 & Global)**: Se implementó la limpieza de enlaces Markdown en la normalización de partidos e integración de resultados reales web para resolver el 100% de las 118 alertas acumuladas (0 alertas pendientes). La **Regla 3: Sorpresa Tempranera** se actualizó de 70 alertas a **39 GREEN / 29 RED / 2 EVITADAS** (57.4% Win Rate).
  - **Bitácora Completa**: Tabla interactiva con buscador en tiempo real por partido o liga y selectores de filtrado por estado (GREEN, RED, EVITADA) y por regla.
- **[2026-08-10]**: Optimización Estratégica de la Regla 3: Sorpresa Tempranera en `rulesEngine.js` (Versión 2.5.0).
  - **Postergación de Ventana de Disparo**: Se ajustó el tiempo de activación a `elapsed >= 30 && elapsed <= 41` (a partir del minuto 30'). De esta forma, el bot recopila 30 minutos completos de estadísticas de presión en vivo (tiros, posesión, ataques peligrosos) antes de emitir la recomendación.
  - **Inclusión de Mercado Ambos Anotan (BTTS)**: Se habilitó la recomendación explícita de "Ambos Anotan (BTTS)" y "Próximo Gol del Favorito" sin prohibir mercados existentes (permitiendo Doble Oportunidad cuando las métricas en vivo confirmen la reacción).
  - **Filtro de Confianza Mínima (65%)**: Se fijó el parámetro `minConfidence: 65` para exigir un umbral mínimo del 65% en las predicciones generadas para la Regla 3.
- **[2026-08-11]**: Desactivación Definitiva del Envío de Documentos de Prompts a Telegram (Versión 2.5.1). Se comentó el bloque de código `sendDocument` en [index.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/index.js) a solicitud del usuario para dejar de enviar los archivos de texto de prompts (`prompt_gemini_*.txt` and `prompt_deepseek_*.txt`) al canal "Los prompts de golesytarjetas". Se incrementó la versión en `package.json` a v2.5.1.
- **[2026-08-13]**: Auditoría Integral y Resolución Web de Apuestas en `messages.html` (Versión 2.5.2).
  - **Caché Local de Búsquedas (`resultados_cache.json`)**: Se implementó una base de datos de caché local para almacenar los resultados reales de partidos que no cuentan con un veredicto directo en los logs. Esto evita el consumo innecesario de peticiones a la API y el error 429 de cuota en Google Search Grounding de la cuenta gratuita de Gemini.
  - **Normalización de Nombres de Partidos**: Se robusteció la función `normalizarPartido` en `generar_reporte_messages.js` para aplicar primero la normalización diacrítica y luego el filtrado de caracteres especiales, previniendo que se borrasen letras de equipos internacionales (como São, Água o Nõmme) y permitiendo cruzar con éxito el 100% de las 163 alertas registradas (0 pendientes).
  - **Pestaña Interactiva de Auditoría de Fallos**: Se añadió la sección `⚠️ Auditoría de Fallos` en `reporte_messages.html` que lista detalladamente las 51 apuestas perdidas (`RED`), con detalles del marcador, confianza e hipótesis recomendadas por Gemini y DeepSeek para facilitar análisis y ajustes del bot.
  - **Resultados de la Auditoría**: Efectividad global de 68.13% (109 GREEN / 51 RED). Gemini obtuvo 67.10% y DeepSeek 67.72%. Se detectó que la Regla 3: Sorpresa Tempranera arrastra la media del bot (59.3% win rate en 88 alertas) y que el mercado de Doble Oportunidad opera al 50.0% de efectividad en contraste con las Victorias Directas (79.1%).
- **[2026-08-13]**: Optimización Estratégica de Alertas en Vivo y Control Financiero (Versión 2.6.0).
  - **Filtro de Dobles Oportunidades**: Se implementó una lógica de bloqueo en `index.js` que descarta cualquier recomendación en vivo donde Gemini sugiera "Doble Oportunidad" (1X, X2, doble chance, etc.) debido a su bajo rendimiento histórico (47.8%).
  - **SafeOdds Estricto**: Se eliminó el fallback de estimación temporal que enviaba apuestas a ciegas tras transcurrir el tiempo en cola. Ahora, si expira el tiempo sin una cuota real confirmada de @1.60 por la API, la alerta se cancela y descarta definitivamente de la cola, protegiendo el retorno matemático.
  - **Regla 3 al 75%**: Se elevó el umbral `minConfidence` de la Regla 3: Sorpresa Tempranera a `75` en `rulesEngine.js` para reducir ruido, reflejando el cambio de forma dinámica en la evaluación de IA y en los textos del mensaje enviado a Telegram.
  - **Pruebas de Integración**: Se corrieron exitosamente las simulaciones locales de `test.js` y `testAi.js` validando la robustez de los cambios y fallbacks ante contingencias de red.

- **[2026-08-13]**: Detección y Alertas de Corrección VAR en Vivo (Versión 2.7.0).
  - **Soporte para Goles Anulados**: Se implementó una lógica de comparación que detecta en tiempo real si el marcador en vivo retrocede con respecto al registrado al momento del envío de la alerta, notificando de inmediato una alerta de gol anulado.
  - **Soporte para Tarjetas Rojas Anuladas**: Se adaptó el cliente API para adjuntar el estado de error (`isError`) a los eventos. Si se detecta que una tarjeta roja previamente alertada desaparece de los eventos activos del partido, se notifica la anulación de la expulsión.
  - **Persistencia Antiduplicados**: Se implementó la función `updatePlayMetadata` en `financialTracker.js` para persistir las correcciones del VAR en el archivo local `financial_tracker.json`. Esto previene notificaciones duplicadas tras reinicios.
  - **Pruebas de Integración**: Se ejecutó exitosamente el script `scratch/test_var_corrections.js` validando todos los escenarios de corrección, control de spam e inmunidad contra fallos de red.

- **[2026-08-13]**: Optimización de Prompts y Regla de Valor en Línea de Goles en Vivo (Versión 2.7.1).
  - **Evitar Apuestas de Bajo Valor**: Se introdujo una regla crítica de valor en líneas de goles en vivo (`REGLA DE VALOR EN LÍNEAS DE GOLES (CRÍTICA)`) en los prompts de fútbol de Google Gemini y DeepSeek en `aiService.js`.
  - **Línea Dinámica vs Marcador**: Se prohíbe explícitamente sugerir una apuesta de goles "Más de (G + 0.5)" si el marcador en vivo ya cuenta con G goles (como sugerir "Más de 1.5" si va 1-0), ya que ofrece cuotas ínfimas en vivo (menores a @1.30). Se instruye a la IA a sugerir líneas con valor como "Más de (G + 1.5)" (Over 2.5 en este caso), "Más de (G + 0.5) en Primer Tiempo" u otros mercados válidos con momio mínimo de @1.60+.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.1`.

- **[2026-08-15]**: Optimización de Ligas Menores y Fallback de DeepSeek por Descarte de Gemini DO (Versión 2.7.2).
  - **Ligas Menores**: Se eliminó la validación obligatoria de momios en vivo para partidos de ligas menores (`!isTop`). Ahora, estas alertas se envían de inmediato y se les añade un aviso aclaratorio en el mensaje de Telegram (`⚠️ Nota: Este partido pertenece a una liga menor (se envía de inmediato sin validar cuotas en vivo)`), evitando que queden retenidas y expiren en la cola de SafeOdds debido a la falta de cobertura en vivo de la API.
  - **Consenso Dual de IA**: Si Gemini recomienda una apuesta de Doble Oportunidad (DO), el bot descarta su recomendación de forma individual (tachándola y marcándola con un emoji en el mensaje unificado de Telegram), pero no aborta el partido completo. En su lugar, el bot activa la propuesta de DeepSeek (`deepseekRecommendation`) como la recomendación activa principal para el flujo de SafeOdds y el control financiero, permitiendo que la alerta continúe adelante con la recomendación de DeepSeek.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.2`.

- **[2026-08-15]**: Corrección en el flujo de alertas y SafeOdds ante fallas de la IA (Versión 2.7.3).
  - **Solución al silencio de alertas**: Se detectó en los logs de Railway que cuando las llamadas a la IA (Gemini/DeepSeek) fallaban por errores `503` (cuotas de API gratuita) o `404` (nombre de modelo no disponible), las alertas completas se perdían por completo. Esto se debía a que toda la lógica de SafeOdds, envío a Telegram y registro financiero estaba dentro del bloque `if (aiPrediction)`.
  - **Fallback Estático y SafeOdds**: Se reestructuró la lógica en `index.js` para extraer el bloque de SafeOdds y el envío a Telegram fuera del bloque condicional de la IA. Si la IA falla, se activa un fallback estático que recupera la recomendación predeterminada escrita en el texto de la regla original mediante regex y procesa la alerta de manera normal (enviando el texto estático sin análisis de IA).
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.3`.

- **[2026-08-15]**: Integración de DeepSeek como Fallback predictivo directo (Versión 2.7.4).
  - **Uso prioritario de DeepSeek ante fallas de Gemini**: A solicitud del usuario, si la llamada a la API de Gemini falla o devuelve nulo, el bot ya no pasará de inmediato al fallback estático. En su lugar, intentará consultar a la API de DeepSeek para procesar la recomendación activa e integrarla a SafeOdds.
  - **Consistencia de Fallbacks**: Si tanto Gemini como DeepSeek fallan (por problemas de red o saldo), el bot recurrirá al fallback estático de la regla original como última línea de defensa.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.4`.

- **[2026-08-15]**: Corrección de falsos positivos en tarjeta roja anulada por el VAR (Versión 2.7.5).
  - **Validación de Eventos no Vacíos**: Se añadió una restricción para evitar la detección de anulación si el array de eventos retornado por la API está vacío (`events.length === 0`), lo cual suele deberse a caídas temporales de la API-Sports o retrasos de sincronización y no a una corrección real del VAR.
  - **Doble Verificación con Estadísticas**: Al detectar una posible anulación, se implementó una consulta asíncrona a las estadísticas en vivo del partido mediante `getMatchStatistics(fixtureId)`. Si las estadísticas confirman que el equipo afectado mantiene un número de tarjetas rojas mayor a 0, se descarta el aviso de anulación (falso positivo).
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.5`.

- **[2026-08-16]**: Corrección de modelos de Gemini API por obsolescencia en Google Cloud (Versión 2.7.6).
  - **Reemplazo de Modelos**: Se sustituyó el modelo deprecado `gemini-2.5-flash` (que devolvía error 404 continuo) por el modelo actual compatible `gemini-3.5-flash`.
  - **Fallback Robusto**: Se reorganizó la lista de modelos prioritarios en `aiService.js` a `['gemini-3.5-flash', 'gemini-flash-latest']`, previniendo que los timeouts y límites de cuota (429) en los alias inhabiliten por completo el servicio de IA del bot.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.6`.

- **[2026-08-16]**: Corrección y optimización del sistema de evaluación de veredictos post-partido (Versión 2.7.7).
  - **Soporte robusto para Fallback de DeepSeek**: Se corrigió el flujo en `rulesEngine.js` para que si Gemini falla en la alerta original (`geminiRecommendation` es `'N/D'`) pero DeepSeek genera una predicción válida, el veredicto post-partido se evalúe correctamente utilizando el resultado de DeepSeek, activando la bandera `evaluatedByAI = true` y evitando falsos GREENs provocados por el descarte en el fallback tradicional.
  - **Filtro de strings informativos 'N/D'**: Se previno que se envíen cadenas como `'N/D (Gemini falló)'` o `'no disponible'` a la API de Gemini para evaluar si resultaron ganadoras. Esto optimiza el consumo de la API en fútbol y béisbol (`baseballRulesEngine.js`).
  - **Corrección de Regla 1 Tradicional Estática (Tarjeta Roja)**: Se implementó la lógica real en el switch tradicional estático para que el veredicto sea `RED` si el equipo perjudicado (el que recibió la tarjeta roja) mantiene la victoria al final del encuentro. Ahora solo marcará `GREEN` si el equipo perjudicado no ganó (el beneficiado empató o ganó). Se aplicó la misma validación al cálculo de control de omisiones.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.7.7`.

- **[2026-08-17]**: Auditoría completa y actualización de rendimiento sobre el nuevo archivo `messages.html`. Se ejecutó el script `scratch/generar_reporte_messages.js` para extraer y consolidar las alertas del bot. Se procesaron un total de 241 alertas y se resolvieron 227 veredictos, quedando 14 pendientes debido a límites de cuota (429/404) en las consultas de Gemini Search Grounding. El reporte interactivo `reporte_messages.html` y el caché local `resultados_cache.json` fueron actualizados exitosamente, reportando un Win Rate Global de **72.44%** (163 GREEN / 62 RED / 2 Evitadas).

- **[2026-08-18]**: Desactivación Definitiva de la Regla 3: Sorpresa Tempranera (Versión 2.8.0).
  - **Desactivación de Alertas**: Se comentó la lógica de evaluación y encolado de alertas de la Regla 3 en la función `evaluateRules` de [`rulesEngine.js`](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/rulesEngine.js). Esto previene que el bot genere alertas en vivo de esta regla y evita el desgaste de capital debido a su bajo rendimiento histórico (59.6% Win Rate).
  - **Retrocompatibilidad**: Se mantuvo intacto el bloque del switch `case 3` en `evaluateAlertResults` para permitir la resolución automática de veredictos y omisiones de partidos históricos.
  - **Pruebas de Integración**: Se ejecutó exitosamente `test.js` para garantizar la estabilidad de las reglas activas.
  - **Control de Versión**: Se actualizó la versión en `package.json` a la versión `2.8.0` por remoción de funcionalidad (SemVer).

- **[2026-08-19]**: Actualizacin del sistema de evaluacin de apuestas para soportar marcadores nulos/reembolsados. Se modific \evaluatePredictionOutcome\ y \
esolveVerdictViaWeb\ en \iService.js\ para poder retornar \VOID\ mediante \isVoid\ en lugar de forzar a GREEN o RED. En \
ulesEngine.js\, se adapt el veredicto final dual de Gemini y DeepSeek, mostrando ? BLACK cuando el resultado de la apuesta es anulado (VOID o CANCELLED) en lugar de un veredicto RED incorrecto.

### 20 de Agosto 2026
- Se recibió actualización de `messages.html`.
- Se generó un script de extracción en Python para recopilar datos de rentabilidad y resultados de inteligencia artificial.
- Se produjo el reporte web `reporte_actualizado.html` estructurado con comparaciones Gemini vs DeepSeek y despliegue por cada regla operativa.


- **[2026-08-21]**: Desactivacion del monitoreo de ligas menores (Version 2.8.2).
  - **Filtro de Ligas Menores**: Se anadio un descarte temprano en la funcion checkMatches() en [index.js](file:///c:/Users/sergi/.gemini/antigravity/scratch/rojas%20y%20goles/index.js) para omitir el monitoreo de partidos pertenecientes a ligas menores (!isMajorLeague(match.league)). Esto evita el desperdicio de peticiones API y tiempo de procesamiento en partidos con escasa cobertura estadistica y sin cuotas en vivo.
  - **Control de Version**: Se actualizo package.json a la version 2.8.2.

- **[2026-08-24]**: Auditoría manual de resultados de inteligencia artificial (Gemini vs DeepSeek).
  - **Análisis Real**: Se evaluó una muestra representativa de predicciones de IA descartando los resultados locales de Telegram en `messages.html` y comparándolos con los marcadores reales extraídos de internet. DeepSeek demostró un mayor porcentaje de efectividad (aprox. 66%) frente a Gemini, gracias a su enfoque pragmático en el mercado de goles (ej. 'Siguiente gol' y 'Más de 2.5') en contraste con la aproximación agresiva y dependiente de mercados secundarios (tarjetas/córners) de Gemini.
  - **Generación de Reporte**: Se guardó la evaluación detallada en el archivo `reporte_auditoria_messages.html` para revisión del usuario.
  - **Pivote Estratégico**: Con base en estos hallazgos, el usuario determinó enfocarse exclusivamente en DeepSeek como el motor de IA principal para futuras predicciones.
  - **Mejora del Motor DeepSeek**: Se actualizó el arreglo de modelos de fallback en `aiService.js` para priorizar `deepseek-reasoner`. Esto eleva las capacidades lógicas del bot al forzar cadena de pensamiento (Chain of Thought) antes de emitir un pronóstico, minimizando alucinaciones y subiendo la precisión en apuestas en vivo. `deepseek-chat` queda como primer fallback.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.8.3` por mejora en el motor lógico.

- **[2026-08-27]**: Eliminacion Completa de Modulos y Referencias de Beisbol (Version 2.9.0).
  - **Eliminacion de Archivos**: Se eliminaron los archivos `baseballApiClient.js`, `baseballRulesEngine.js` y `testBaseball.js`.
  - **Refactorizacion de `aiService.js`**: Se eliminaron las funciones `formatBaseballStats`, `buildBaseballPrompt`, `buildBaseballPromptDeepSeek` y `formatBaseballFinalData`. Se simplificaron `generatePrediction`, `generatePredictionDeepSeek`, `buildDailyParlayPrompt`, `evaluatePredictionOutcome` y `resolveVerdictViaWeb` para procesar unica y exclusivamente datos de futbol.
  - **Limpieza en `index.js` y Pruebas**: Se eliminaron importaciones residuales, la variable de cache `currentLiveBaseballIds`, comentarios huerfanos de MLB y la logica condicional de deportes en parlays. Se limpiaron `test_omission.js` y `testAi.js`.
  - **Validacion**: Se ejecutaron exitosamente los tests unitarios (`node test.js` y `node test_omission.js`), comprobando el 100% de operatividad en las reglas de futbol y la verificacion GREEN/RED.
  - **Control de Version**: Se incremento la version a `2.9.0` en `package.json` siguiendo el estandar SemVer.

- **[2026-08-27]**: Ajuste de Intervalo de Minutos en Regla 1: Tarjeta Roja Estratégica (Versión 2.9.1).
  - **Ajuste de Rango Temporal**: Se modificó la condición de activación de la Regla 1 en `rulesEngine.js` para que solo opere entre los minutos 35 y 72 (`elapsed >= 35 && elapsed <= 72`), sustituyendo el rango previo (`elapsed < 60`).
  - **Optimización de Consulta de Estadísticas y Eventos**: Se ajustaron `needsStats` y `needsEvents` en `rulesEngine.js` para consultar datos hasta el minuto 85 continuo, cubriendo el nuevo rango sin vacíos entre los minutos 60 y 70.
  - **Pruebas de Validación**: Se añadieron pruebas de límites de tiempo en `test.js` verificando que la alerta no salte al minuto 30 o 75, y que active correctamente en los minutos 50 y 72.
  - **Control de Versión**: Se actualizó `package.json` a la versión `2.9.1` (SemVer).

- **[2026-08-27]**: Migracion Exclusiva a DeepSeek y Remocion Total de Google Gemini (Version 2.10.0).
  - **Servicio de IA (`aiService.js`)**: Se elimino toda la infraestructura de Google Gemini (lectura de `GEMINI_API_KEYS`, rotacion de claves, modelos `gemini-3.5-flash`/`gemini-flash-latest`, prompt dual y llamadas Google Search Grounding). Se unificaron todas las operaciones (`generatePrediction`, `generateDailyParlay`, `evaluatePredictionOutcome` y `resolveVerdictViaWeb`) para operar de manera exclusiva y robusta con la API de DeepSeek (`deepseek-reasoner` y `deepseek-chat`).
  - **Orquestador (`index.js`)**: Se eliminaron las consultas duales paralelas y el formato hibrido de Telegram. Ahora las alertas se analizan y presentan directamente bajo el bloque exclusivo de DeepSeek (DEEPSEEK), aplicando los filtros de confianza y calidad directamente sobre su salida.
  - **Motor de Reglas y Veredictos (`rulesEngine.js`)**: Se simplifico la evaluacion post-partido para calificar directamente los pronosticos de DeepSeek y emitir un mensaje de veredicto limpio y unificado (DEEPSEEK: GREEN/RED/BLACK).
  - **Configuracion**: Se elimino `GEMINI_API_KEYS` de `.env.example` y se actualizo `financialTracker.js`.
  - **Control de Version**: Se incremento la version a `2.10.0` en `package.json` siguiendo el estandar SemVer.

- **[2026-08-27]**: Expansion de Ligas Top y Copas Nacionales de Elite (Version 2.10.1).
  - **Ampliacion de Torneos Monitoreados**: Se agregaron a `config.js` las copas y ligas competitivas de primer nivel con cobertura estadistica completa y alta liquidez en casas de apuestas: Championship, FA Cup, EFL Carabao Cup, Copa del Rey, Coppa Italia, DFB-Pokal, Coupe de France, Leagues Cup, CONCACAF Champions Cup, Copa do Brasil, Copa Argentina, UEFA Super Cup, UEFA Nations League y FIFA Club World Cup.
  - **Control de Version**: Se actualizo `package.json` a la version `2.10.1` (SemVer).

- **[2026-08-27]**: Integracion de la Liga Profesional Saudi y AFC Champions League (Version 2.10.2).
  - **Inclusion de la Liga Saudita**: Se agregaron a `config.js` la Saudi Pro League (ID 307), la King Cup (ID 308) y la AFC Champions League (ID 17), reconociendo sus palabras clave (`saudi pro league`, `roshn saudi league`, etc.) para aprovechar su alta media de goles, cobertura de stats completa en vivo y mercados abiertos en casas de apuestas.
  - **Control de Version**: Se actualizo `package.json` a la version `2.10.2` (SemVer).

- **[2026-08-27]**: Eliminacion Definitiva de la Regla 3: Sorpresa Tempranera (Version 2.11.0).
  - **Limpieza de Regla de Bajo Rendimiento**: Se removieron por completo la definicion de la Regla 3 y su evaluacion post-partido (`case 3`) en `rulesEngine.js`. Esta decision responde a su bajo rendimiento historico (~59% Win Rate), eliminando ruido en las alertas y elevando la efectividad proyectada del bot globalmente.
  - **Actualizacion de Pruebas**: Se actualizo `test_omission.js` para emplear reglas activas (`ruleType: 5`) en las verificaciones de omisiones.
  - **Control de Version**: Se incremento la version a `2.11.0` en `package.json` (SemVer).

- **[2026-08-27]**: Correccion de Sintaxis en Bloque Try/Catch de `index.js` (Version 2.11.1).
  - **Correccion de Error de Sintaxis**: Se removio un bloque de cierre prematuro `catch (e)` residual en la seccion de IA de `index.js` que ocasionaba un error de arranque en Node.js (`SyntaxError: Unexpected token 'catch'`).
  - **Validacion de Sintaxis**: Se comprobo con `node --check` todos los archivos JavaScript del proyecto (`index.js`, `aiService.js`, `rulesEngine.js`, `config.js`, `financialTracker.js`).
  - **Control de Version**: Se incremento la version a `2.11.1` en `package.json` (SemVer).

- **[2026-08-27]**: Reescritura de SafeOdds: Eliminación de Expiración por Tiempo y Limpieza de Ligas Menores (Versión 2.12.0).
  - **Eliminación de Cancelación por Expiración**: Se eliminó por completo el descarte por tiempo límite (`waitMinutes`) en `processPendingAlerts()`. Ahora las alertas encoladas en `pendingAlertsQueue` permanecen activas esperando a que la cuota en vivo alcance o supere `@1.60` durante todo el tiempo que el partido continúe en juego y no haya cambio de marcador.
  - **Limpieza de Referencias a Ligas Menores**: Dado que el bot opera exclusivamente sobre torneos y copas mayores de élite (`isMajorLeague`), se removieron todas las variables, condiciones y avisos residuales (`isMinorLeague`) en el flujo de despacho de alertas y SafeOdds en `index.js`.
  - **Validación Automatizada**: Se crearon y ejecutaron pruebas de integración (`scratch/testSafeOddsRefactor.js`) y regresión (`test.js`), validando exitosamente la persistencia de alertas sin expiración temporal, activación por momio `@1.60+`, cancelación por gol y cancelación por partido finalizado.
  - **Control de Versión**: Se incrementó la versión a `2.12.0` en `package.json` siguiendo el estándar SemVer.

